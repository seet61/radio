var DB = function(host, port) {
  this.host = host;
  this.port = port;
};

DB.prototype.say = function() {
  console.log('host ' + this.host);
};


module.exports = new DB();
//module.exports.say = say;
